# Setup Project

Create .env File :

```
DATABASE_URL="mysql://pass:@localhost:port/database_lu"
```

Terminal Command :

```shell
npm install

npx prisma migrate dev

npx prisma generate

npm run build

npm run start
```
